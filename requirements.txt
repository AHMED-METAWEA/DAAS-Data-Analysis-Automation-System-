langgraph>=0.2.0
langchain-core>=0.3.0
groq>=0.11.0
anthropic>=0.40.0
openai>=1.50.0
pandas>=2.1.0
numpy>=1.24.0
plotly>=5.20.0
psycopg2-binary>=2.9.0
pydantic>=2.0.0
python-dotenv>=1.0.0
scipy>=1.11.0
sqlalchemy>=2.0.0
alembic>=1.13.0
cachetools>=5.3.0
gspread>=6.1.0
google-auth>=2.30.0
cryptography>=42.0.0
pymysql>=1.1.0
pyodbc>=5.0.0
scikit-learn>=1.3.0
shap>=0.45.0
prophet>=1.1.6
statsmodels>=0.14.0
openpyxl>=3.1.0
pyarrow>=15.0.0

# Backend API (FastAPI)
fastapi>=0.115.0
# Floor set explicitly because `fastapi>=0.115.0` alone resolves anywhere from
# Starlette 0.37 to current, and CORSMiddleware's `allow_private_network` — needed so
# Chrome's Private Network Access preflights aren't refused with a bare 400 — only
# exists from 0.51.0. backend/app/main.py degrades gracefully without it, but two
# machines resolving different Starlettes is itself how "works here, not there" starts.
starlette>=0.51.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pyjwt>=2.8.0
bcrypt>=4.1.0
email-validator>=2.1.0

# Scheduled monitoring & proactive alerts (Pillar III-1)
apscheduler>=3.10.0
# HTTP client for the Telegram / WhatsApp (Meta Cloud API, Twilio) channels.
httpx>=0.27.0
# Windows ships no system IANA timezone database, so zoneinfo cannot resolve
# "Africa/Cairo" without this — every schedule would silently fall back to UTC.
tzdata>=2024.1; sys_platform == "win32"
